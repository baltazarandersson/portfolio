---
import type { HTMLAttributes } from 'astro/types'

interface Props extends HTMLAttributes<'span'> {}

const { class: className, ...attrs } = Astro.props
---

<span {...attrs} class:list={['inline-flex items-center justify-center', className]}>
	<slot />
</span>
