---
import { SECTION_CLASS } from "@constants/index";

interface Props {
  class?: string;
  id?: string;
}

const { class: className = "", id = "" } = Astro.props;
---

<section class={`min-h-screen py-20 w-full ${className} ${SECTION_CLASS}`} {id}>
  <slot />
</section>
