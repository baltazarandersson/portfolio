---
import type { JobType } from 'types/jobs'
import JobCard from './JobCard.astro'

interface Props {
	jobs: JobType[]
}

const { jobs } = Astro.props
---

<ul class="radiant flex w-full flex-col gap-8 lg:w-2/3">
	{jobs.map((job) => <JobCard {job} />)}
</ul>
